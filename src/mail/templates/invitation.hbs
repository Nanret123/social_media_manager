<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Organization Invitation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9fafb;
        padding: 20px;
        color: #111827;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 8px;
        padding: 24px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.08);
      }
      h2 {
        color: #1f2937;
      }
      p {
        line-height: 1.6;
      }
      .button {
        display: inline-block;
        margin-top: 20px;
        padding: 12px 20px;
        background: #2563eb;
        color: white;
        text-decoration: none;
        border-radius: 6px;
        font-weight: bold;
      }
      .footer {
        margin-top: 30px;
        font-size: 12px;
        color: #6b7280;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>You’re Invited to Join {{organizationName}}</h2>

      <p>Hello,</p>

      <p>
        {{inviterName}} has invited you to join the <strong>{{organizationName}}</strong>
        organization as a <strong>{{role}}</strong>.
      </p>

      <p>
        To accept this invitation, click the button below. This invitation will
        expire in 7 days.
      </p>

      <a
        href="{{frontendUrl}}/invitations/accept?token={{token}}"
        class="button"
      >
        Accept Invitation
      </a>

      <p>If the button above doesn’t work, copy and paste the link below into your browser:</p>
      <p>{{frontendUrl}}/invitations/accept?token={{token}}</p>

      <div class="footer">
        <p>If you weren’t expecting this email, you can ignore it.</p>
        <p>© {{year}} {{organizationName}}. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
